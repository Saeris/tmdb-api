export { gender } from "./shared";

export const creditId = (parent: Record<string, string>) => parent.credit_id;

export const job = (parent: Record<string, string>) => parent.job;

export const photo = (parent: Record<string, string>) => parent.profile_path;
